# 更新记录

项目仍处于0.x开发阶段，尚未选定开源许可证。版本采用SemVer；可体验功能批次发布，不为每次临时改动递增。

## 0.4.2 — 2026-09-25

### 修复

- 每日复习不再被实验分配卡死：gap_retrieval_v3 会话只在按题范围生效；此前到期实验块会让当日复习入口反复抛 409 且会话无法恢复。
- 学习会话音频 GET 改为只重定向已缓存音频并加本机同源守卫；GET 不再写请求记录或触发付费合成，新生成仍走本机 POST。
- 全部写接口 JSON 体统一经过回环 Host／同源／Content-Type 校验：跨站简单请求不能再驱动创建回答、AI 任务等 25 个变更端点；畸形 Host 返回 403。
- 含媒体加密业务备份补齐个人学习数据：v2 学习进度与结算、个人 Gap 诊断台账、V3 提取历史、个人词块与例句、题目练习/掌握状态、口语会话、回答导入归档。此前换机恢复会静默丢这些进度。
- 三处语音听写（FreeTalk、口语输出、回答工作台）在听写期间手动编辑的文本不再被下一帧转写覆盖。
- 服务端写入路径加固：回答版本号检查移入事务（并发保存得到干净的 409 而非 500）、AI Job 状态转换校验真实生效、损坏的运行回执按失败处理而非裸异常、原声备注缺失返回 400 不再静默清空、原声重复上传不再卡在 publishing、备份分块越界明确拒绝、个人词块管线单事务入库、未知题目掌握状态返回 404。
- 前端静默失败路径收敛：回答轮询失败不再抛未处理异常、恢复按钮锁定可靠释放、Gap 老师面板失败不再无限重试、Enter 不再在输入法组词时误发送。

### 性能与数据

- FreeTalk 消息镜像改为按游标追加，不再每条消息全量重写（旧实现随对话长度 O(N²)）。
- v37 追加迁移为轻学习事件表建 learning_item_id／created_at 索引，首页"未完成"探针不再逐行全表扫描；只增索引，升级前照常备份。
- 轻学习目录读取范围化未纳入本版：现实规模实测 overview p95 约 64ms，其余收益仅在十倍合成规模，留待后续评估。

### 验证与限制

- 本机统一质量门：治理、Lint、类型、649 单测、32 集成文件、Golden、构建、两类材料审计退出 0；E2E 20 项通过后既有 Windows 服务原生退出（0xC0000005），按既定边界不调查，完整门以 Linux CI 为准。
- 开发 Runtime 请求 0；无新功能，不改变学习合同与评分语义。

## 0.4.1 — 2026-09-16

- 修复整题学习页与暂停页把中文题名当主标题的问题：有题库英文原题时优先展示英文，中文作为次要提示；历史材料无需重新生成，学习记录不变。
- 声音缓存仍由本机持久WAV承担。本版未批量合成、未更换语音模型或声称修复首次生成延迟；后续将分别评估流式MiMo、按题主动准备和可选本地TTS。

## 0.4.0 — 2026-09-16

### 新增与调整

- 整题同页中英对照、主动逐词揭晓；已学句子仍保留在完整上下文中。草稿、教学、高亮、就地再练与老师帮助不以评分为门槛。
- 首页明确选择继续、自己选题或自动选题，暂停后可直接换题。首次实际接触但未评分的句子安排约24小时后回想，正式自评沿用独立FSRS。
- 完整回答可录音或上传原文件，纯音频可独立保存；本机分片暂存、可恢复上传，不转写、不做声学评分或自动调用AI。
- 题目回答原声历史、任意两次A/B原速回听、下载、备注、可恢复移除。历史无录音记录保留，草稿与局部修正不冒充完整回答次数。
- 含原声的v2流式加密备份，支持超过64MiB的总媒体，完整认证后恢复、删除标记优先，不包含API Key或示范声音缓存。
- 学习Milo／老师Chloe默认美式，角色声线与口音独立、倍速共享；录音时禁止软件示范插播。
- 当前句子材料集合、搜索、收藏、备注、暂不学和内容反馈；相关题目练习与有来源的老师记忆贯通。

### 安全与兼容

- v36追加迁移，升级前备份，保留原回答、历史材料、旧评分、高亮与拓展模式。正文未全库重新生成。
- 开发Runtime调用0；录音自动化使用模拟采集器和合成音频，不使用真实麦克风。
- 当前真实材料只读审计105份／593句／3处待确认，无新增审计问题。私人材料、原声和审核正文不随源码分发。
- 完整检查与定向补验分别记录于PROJECT_STATUS；本机已知Windows原生退出未展开调查。声音听感与学习提效仍由实际体验判断。

## 0.3.0 — 2026-09-11

### 新增

- 句子回想页加入磨砂遮挡英文、逐词跟手揭晓和可选草稿，支持键盘、触屏、重新遮挡；不自动跳页、播放或评分。
- “这句怎么说”提供完整意义讲解、可复用结构、双语迁移例句、词义辨析和带使用条件的自然替代；在生成材料时准备并独立审核，学习点击不等待AI。
- 三档自评改为“脱口而出／想出来了／遇到卡壳”，评分后留在本句，用户主动选择下一句或本地再练。
- 再练隐藏答案及前次提示，揭晓后对照自己的输入；不重复结算FSRS。老师反馈需显式选择，不自动消费API。
- v35追加版本化教学记录，纳入业务备份；既有高亮、原回答、旧版本与学习证据保留。

### 修复

- 首页恢复旧会话先对账原事件，再升级体验；草稿、揭晓位置、评分与推进分别保存，避免丢响应后重复评分或跳错句。
- 修复并发草稿提交与迟到回执覆盖新输入；保留最后一句评分后的再练和修改机会。
- 修复教学任务恢复时被旧Schema剥掉讲解字段的问题；新旧Prompt和审核结果按创建合同隔离。
- 对既有材料定向修订词义范围、中英文对齐和失效用法映射；仅更新讲解不重置学习成绩，内容实质改变不继承旧句成绩。

### 内容与验证边界

- 本机105份当前材料、594句全部离线制作并独立审核；593句具备教学，1句器具名称无法确认，已隔离。另有此前保留的2个待确认单元。私人材料及审核正文不随公开源码发布。
- 开发期Runtime调用0；真实服务验证未追加。声音听感与实际学习提效仍由用户体验确认。
- 自动化、内容审核和界面证据分开记录，最终命令及CI回执见docs/PROJECT_STATUS.md。安卓与同步继续冻结，Windows既有原生退出不展开调查。
- 当前规模概览及创建/恢复已有亚秒级测量证据；十倍合成资料下概览仍有约4.7秒尾延迟，未宣称所有规模都即时。浏览器已准备的揭晓/切句不等待这些查询。

## 0.2.0 — 2026-09-11

本轮主要实现与本机验证完成于2026-09-10；发布日期按北京时间记录。

### 新增

- 中文提示和英文句子的个人黄色高亮，支持保存、取消、刷新恢复和业务备份；不改变学习评分。
- MiMo Milo年轻美式男声，声线与口音独立，0.85×/1×/1.1×/1.2×播放倍速无需重新合成。
- 当代自然美式表达配置；推荐补充中文原意但允许跳过。
- 经过审核的原错误与纠正证据接入老师记忆，保留来源、删除和撤销能力，不自动把风格偏好或准备项记为错误。

### 修复

- 升级DeepSeek V4.1 Flash（官方API名deepseek-flash），修复严格结构参数、实际返回模型识别和流式结果读取。
- 更充足的材料阶段输出预算，保留真实失败原因及已知消耗；参数错误不再无效循环重试，未知请求需明确确认。
- 旧失败材料可在保留原回答及旧记录的前提下升级当前生成合同，重复重试不重复创建回答或材料版本。
- 审核ID限定为本次诊断／材料的闭集；无效审核不再连带重付诊断。实际已批准的四阶段缓存可离线恢复发布，不再重复调用模型。
- 句子学习不再受旧语块的逐字复写、句末标点限制；混合输入按实际来源字段校验。准备项不冒充英语错误，不要求不存在的“最小纠错”标记。
- 高亮换版与跨窗口缓存安全恢复；当前声音、停止及偏好保持一致。

### 验证边界

- 原失败阅读回答已用真实V4.1完成诊断、独立选材、9句材料及独立最终审核；10个用法区分6个确认问题与4个准备项。19次授权请求内完成，最后入库复用原始positive回执、零新增网络；原回答与学习进度保留。
- 自动化采用Mock与隔离数据库；真实API验证、语音试听、内容审核分别记录，详见docs/PROJECT_STATUS.md。
- 真实声音是否合适、学习是否提效仍由用户体验决定；无原始音频不提供发音、流利度或完整雅思口语分数。
- 安卓、同步冻结；Windows偶发原生退出为既有风险，本轮未展开调查。

## 0.1.0 — 历史开发版本

句子学习默认路线、独立复习调度、暂停与评分更正、连续老师输出及安全公开快照。旧版本没有独立Release，不补造发布日期或测试记录。
