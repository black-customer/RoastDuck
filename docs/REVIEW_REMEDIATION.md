# 2026-09-03 全项目 Review 修复契约

> 状态：历史审查修复记录；不把退役产品要求重新列为当前任务。

基线 `e8489ed`，当前执行者串行修复；原有未提交报告与 next-env.d.ts 保留。原始完整报告位于本机 `test-results/review-20260903/REPORT.md`。本清单只修复已核实缺陷，不代替公共内容全量语义复审、私人材料后续编译、原生缩放、真实语音和七天验收。

| 编号 | 修复契约 | 必需回归 |
| --- | --- | --- |
| 1/2 | 旧质量脚本不再生成批准、不删除审核包，只提示标准离线流程 | 有/无待审任务都不改旧文件与数据库 |
| 3 | 发布审计验证现存 checkpoint、提交/输入/输出/Prompt 哈希、独立角色、逐项裁决及当前内容；历史缺证据明确失败 | 删除版本、修改内容/文件、未知 run、套话规则证据均拒绝 |
| 4 | async Reviewer 验证完成后必须继续逐项交叉校验 | 本批外/重复/缺失裁决与越权编辑拒绝 |
| 5 | 公共去重限定公共词书；有学习/个人引用的实体禁止破坏性合并 | 私人 Chunk 与进度、来源不丢失 |
| 6 | 公共批次无法经 Runtime 入口请求模型或写 ai_runs | 生产 drain 立即拒绝；仅隔离 Mock 保留旧预算回归 |
| 7/8 | 学习事件、步骤 CAS、FSRS、日志与按题首轮标记在同一事务结算；同轮唯一 | 并发、故障回滚、重放仅结算一次；学完可重答 |
| 9 | 个人编辑复用内容修订日志，退回待审并失效语境审核，不改原始回答 | 修改不继续调度、重复无变化不增日志 |
| 10 | pending 消息有持久化租约；过期后显式恢复，同 ID 不重复/覆写 | 中断恢复、活跃租约拒绝、旧请求迟到不能覆盖新请求 |
| 11 | 无有效个人关联不调度；按题隐藏与每日隐藏一致 | 隐藏后日/按题队列均排除，公共其他用途保留 |
| 12/13 | 分池取候选，上海日期每日额度扣除已完成量 | 大公共池不饿死个人项；同日续开不超额 |
| 14 | 回答创建重放不倒退已完成状态 | 相同请求重试保留 ready 与版本 |
| 15 | conversing 可展开提示，忙碌状态不可 | 一次教师回复后提示仍可用 |
| 16 | 批次幂等以 stage/unitKey/输入/Prompt 版本判断 | 新版本/内容可重审，同版本重试不重复 |
| 17 | 失败重试按钮独立于英文文本，保留用户草稿 | 中文/英文失败均可重试；桌面/手机真实 UI |

验证使用合成临时数据库；公共证据只读检查使用一致性快照。完整 check 若被历史内容证据阻断，应记录真实非零退出码，不改 Golden/审计条件制造全绿。任何真实数据隔离修复必须先备份、记录命中范围并保持 Chunk ID、原件、回答、学习进度不变。代码修复不授予新的内容批准权。

## 交付结果

17 项代码缺陷均有修复；历史内容恢复仍未完成。发布安全提交 `4333e31`；学习、个人内容和恢复提交 `a2ca5af`。未改 Prompt，不调用真实 DeepSeek / MiMo，不下载模型，不更改真实内容审核状态，不删除任何历史材料或审核包。

| 覆盖 | 代码落点 | 已执行证据 |
| --- | --- | --- |
| 1/2/6 | 退役两个危险 CLI；公共 drain 和共享 AI 服务阻断真实 Provider；公共补全只取公共词书 | publication-remediation：旧文件与 pending 原样保留、退出 1、真实 Provider 调用 0、ai_runs 0 |
| 3/4 | 原件/输入/Prompt/输出/上游核验；撤销已知规则身份；等待 async 后逐项校验；校验与应用同事务 | 合法合成证据正向；越界/缺失/重复/非法编辑、内容漂移、Prompt 篡改和规则身份拒绝 |
| 5/16 | 同书公共去重，已发布或有学习/个人引用时不删实体；批次按完整输入与版本区分 | 跨词书、稳定学习 ID 保留；新输入/版本可再次建批 |
| 7/8 | v14 唯一结算表、事务上下文、步骤 CAS；首轮写回、到期前不反复出同题 | 两个并发事件仅一条日志；故障注入整体回滚；首轮完成、直接结算重放 |
| 9/11 | 共享 amendment 服务；编辑撤销审核和语境证据；隐藏日/按题关联，读取旧会话也排除隐藏内容 | 修订快照；日队列与按题队列同时排除；原文/公共实体不删除 |
| 12/13 | 个人/公共分别取候选；上海日期按剩余新学/复习额度发放 | 大公共池不饿死个人；当天新学及复习上限不重复发放；个人比例集成测试 |
| 10/14/15 | 持久消息租约与显式恢复；旧请求结果隔离；回答幂等不退回 queued；conversing 可提示 | 中断恢复、活跃租约拒绝、迟到请求不能覆写；回答 ready/版本保留；提示可用 |
| 17 | 重试独立于英文文本；保留未保存草稿；过期消息 UI 可恢复 | 英文失败 → 再失败 → 成功；原文/草稿保持；四档宽度、键盘与 Axe |

### 完整质量门（最终轮）

证据目录：`test-results/fix-20260903/workspace/test-results/check-1788445130364/`，机器记录 `check.json`。

| 子门 | 退出码 | 数量 / 结果 |
| --- | --- | --- |
| governance | 0 | 15 项 |
| lint / typecheck | 0 / 0 | 无错误 |
| unit | 0 | 16 文件，132 测试；较基线增加 26 |
| integration | 0 | 10 文件，44 测试 |
| golden | 0 | 6 题、18 原句，失败 0 |
| content-audit | 1 | 2079 条历史公共发布证据失效，核验通过数 0 |
| build:e2e | 0 | 生产构建成功，使用合成临时库 |
| e2e:run | 0 | 14 测试；较基线增加 1 |
| npm run check | 1 | 唯一失败子门为 content-audit |

首轮完整门位于 `check-1788444893824`：新增 E2E 因 Next.js 自带 alert 与业务 alert 的选择器歧义失败；限定业务错误条后最终轮通过。旧个人比例测试缺少有效回答关联，已补真实形状的合成关联，未放宽调度条件。`git diff --check` 正常仓库换行配置下退出 0。

### UI 与数据保护

Impeccable 用于本次有限错误态加固，不重新设计全站。新增重试流程覆盖 320/390/1280/1440px；无横向溢出，按钮可用键盘触发，Axe serious/critical 为 0。已人工查看 320 与 1440px 真实截图；文件为 `workspace/test-results/visual/review-retry-<宽度>.png`。既有 13 个 E2E 同时通过。浏览器原生 200% 缩放、真实语音、本机模型与七天使用仍未验证，不以 CSS 放大或 Mock 冒充。

`test-results/fix-20260903/protected-integrity.json`：54 个受保护文件哈希未变（含真实 DB 文件、私人原件、已有报告、next-env.d.ts 和原有 golden.ts 换行改动）。`integrity-database.json`：真实 56 张表全量内容哈希未变。所有写入测试使用合成临时库；内容门使用 SQLite 一致性副本；隔离副本不包含 .env.local。原有未提交报告和 next-env.d.ts 未暂存、未覆盖。

### 尚不能关闭的内容验收问题

- 2079 条已发布公共内容的严格证据门全部失败。本轮快照首个失败原因分布：1462 条匹配旧规则脚本身份、593 条缺审核包 manifest、5 条缺有效 checkpoint、19 条缺可验证独立 Reviewer 字段。不同缺口可能重叠；此处仅统计每条首个失败原因，结构校验不等同于语义判断。
- 代码门禁已修复，但**没有替这 2079 条做新的独立语义复审，也没有在真实库执行隔离/下架**。它们的旧发布状态和学习进度保留，不能据此宣称内容已安全或全项目验收完成。
- 继续内容修复需按标准离线 Generator → 独立 Reviewer → Coverage Auditor 重建可验证证据；先保存真实库一致性备份并受控撤销失效批准，保留稳定 ID、原句、个人关联和学习记录。不得复用被撤销的规则裁决、删除旧包或仅补身份字段。
- 本轮由单执行者串行完成代码与自检；交付后的独立代码 Review 尚未执行。
