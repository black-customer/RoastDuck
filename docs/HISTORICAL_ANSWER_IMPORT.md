# 历史回答私有导入与追溯
状态：现行数据规范；2026-09-07。本文件只约束原件、切分、匹配和血缘，不规定轻学习步骤或旧词书发布门。

## 私有原件
材料曾从materials目录导入，原件现保留在data/imports/private/ielts-answers/<importId>/original/。已归档材料不再搬移一遍。
原件哈希校验，私人正文不入Git；仓库仅留脱敏清单、版本、偏移、检查点和审核证据。raw_text不覆盖，修订只追加版本。
开发历史处理离线、Runtime调用0；未来网页新导入另按明确范围开发，不冒充当前可用功能。

## 切分与题目
每个源字符有分类和偏移；问题、回答、重答、指令、自评、放弃、ASR疑点和未分类区分。未分类不能默默丢弃。
明确重答单独保留；同一次回答中的元评论不制造新Attempt。精确/别名匹配优先；模糊匹配需相似度至少0.92且首二候选差至少0.08，否则待核实。缺原问句就明确标注，不伪造英文。
v12导入修订保留原片段血缘；v23 practice_source_revisions处理已存回答混题与重答，父原文保留，子记录为连续原文切片。正常列表排除被替代父记录，旧链接仍可查。

## 从原回答到可学材料
来源修订的完成不等于诊断或发布完成。当前诊断/独立选材/四列编译/审核见MATERIAL_SELECTION，离线落库见OFFLINE_PRACTICE_RECOVERY。
不确定ASR不训练，无原音不判断发音或流利度。旧Gap表和旧聚合视图只是兼容证据，不作为轻学习唯一入口。
按题/回答只是筛选，轻学习使用有效practice_materials与稳定learning_item_id。批量大小/复习语义只由LEARNING_EXPERIENCE规定，不再要求8项必学或逐项双语境。

## 运行安全
修订前备份，事务写入与幂等重跑，不破坏原文/版本/进度；证据哈希不匹配时拒绝应用。测试只用隔离库。
旧命令与原行为完整记录在archive/pre-light-study-2026-09-07/HISTORICAL_ANSWER_IMPORT.md；恢复现有数据前先读当前状态和目标脚本，不重跑旧编译。
