# 当前工程真相
更新时间：2026-09-08；分支 codex/web-usability。当前执行 WEB_USABILITY。安卓/同步/签名/公开发布冻结，不继续其验收。
PO最新要求：缩减交付流程，只验证关键路线及数据/费用安全，不将全库检查、多轮Review、逐份文档作为本地交付前置。下面早先完整验收清单保留为未执行范围，不再扩大本轮。
## 基准与数据
- 2026-09-08 PO要求长口语自然度试听：4次MiMo调用均成功，全部Chloe/en-US；同文vlog A/B仅改指令，另有单方电话、播客受访者原创稿。音频30.08/33.44/31.20/33.92秒，合成约6.6–8.1秒；文件及文稿在data/audio/prosody-preview-20260908。只作试听、未加入学习材料、未改变产品默认声线/指令；没有DeepSeek调用或额外重试。听感尚待PO反馈，不把生成成功当作自然度通过。
- 2026-09-08 语音最新实际结果：PO授权代理配置其MiMo Key，已仅保存于Git忽略的.env.local，不回显。后台读取configured=true。只对同一句短句进行了2次真实请求：首次约60.6秒报invalid_audio、未取到音频；修正“接收正文超时误报格式错误”并延长后台等待后，第二次约2.3秒成功。真实MiMo/Chloe/en-US WAV为161324字节、3.36秒，已缓存；缓存HTTP读取两次168ms/36ms，不等于已测完整播放首声延迟。Provider专项5项Mock通过；未批量合成、未调用DeepSeek。以下missing_key段落是此前检查点，现已解决；真人听感与长期延迟尚待体验。
- 最新网页体验检查点 Commit 31984bf，已重启本项目3001服务并返回READY。真实只读复核：schema31，104份回答、294个有效表达、273新项、0到期、0失效；读取前后数据未变，Runtime调用0。用户已有21项轻学习进度保留。
- 后续先由PO体验，不继续扩大验收/文档工作。MiMo仍missing_key；真实自然度待用户在设置填写新Key及小样授权后确认。Windows偶发退出继续列为已知风险，不声称已解决。
- 保全 Commit e13e14e：此前未完成的原生与共享服务工作已保存，不代表安卓完成。当前网页改动仍待本阶段验证提交。
- 开工命令 npm run init 退出码0；备份 data/backups/app.db.init.2026-09-07T18-47-13-005Z.bak，当时v28，稳定ID审计0异常，Runtime网络0。
- 当前新增v29（旧会话继任、语音请求）、v30（跨进程合成互斥）、v31（表达隐藏/收藏/备注与版本反馈）。逐次迁移有备份，不复活Chunk或覆盖历史。
- 先前只读审计104份材料、294个有效表达。当前最新数量必须重新运行只读audit，不将旧数字当最新。
## 实施中，尚未整版验收
- P0：旧V1只读隐藏答案、POST续接V2/5项分批/前驱待确认事件；MiMo实际音源/健康/明确付费重试、跨进程单合成、坏缓存校验、迟到不插播；服务器Key设置不回显；双语持久草稿与明确不会/英文封存。
- P1：材料结果页突出轻松学、四步可选；当前表达搜索/收藏/备注/暂不学/反馈；题目状态读取轻学习，四步独立；无提示重答保留来源，新对照只承认真实原句。
- P2：FreeTalk使用共享Chloe记忆/消息与材料快照，可选择范围后轻松学；记忆撤销与清空防迟到写回；业务加密备份与非覆盖恢复；有效设置与文档收敛。
- 上述为代码实施进度，完整E2E、内容、视觉、独立Review仍在进行，不等于功能全部通过。
## 已执行命令与真实结果
- 最新关键浏览器路线：7项通过、1项旧按钮文案断言失败；草稿刷新/丢响应恢复、明确英文封存、服务器离线本机副本、收藏/隐藏恢复及轻学习新学/复习/有限巩固实际通过。证据test-results/e2e-server-1788815416562与playwright/visual。已查看390px草稿和材料页截图；仅修正标题长度/复选框间距。
- 另一次15项运行：4通过、1旧文案失败，服务以0xC0000005退出导致其余无法连接。证据test-results/e2e-server-1788815224007；此项仍未解决，不归因Node版本、不伪报稳定性。
- 最近构建命令npm run build:e2e退出码0，日志test-results/web-build-checkpoint.log；typecheck/lint与治理定向退出码0。当前完整npm run check未执行，按PO最新要求不作为本地体验版本阻塞。
- 旧会话继任专项6项通过，退出码0；证据 test-results/integration-1788811718408。包含跨范围仍到期快照、原事件幂等和5+3继任。
- 语音请求5项、Web写入边界2项、草稿材料4项通过，typecheck退出码0。两实例同库合成只调用一次，所有网络Mock。
- 一次完整npm test：267通过、3失败（统一记忆Mock输入字段不一致）。修正后companion-service 5项专项通过；未用专项结果冒充完整通过。
- 本机业务加密备份1项、比较原句约束1项专项通过。没有真实API/真人听感/真实效果结果。
- 完整 npm run check 尚未对最终网页工作树执行；之后必须记录完整退出码与证据目录。
## 已确认问题及仍需验证
- 当前服务只读speech/health报告 missing_key。现有机器声音不能归因MiMo质量；需用户在设置输入轮换的新Key后小范围主动试听。开发未调用实际MiMo/DeepSeek。
- Windows偶发0xC0000005根因仍未关闭；已有故障探针，单次通过或更换Node不能宣称解决。网页全量E2E需要继续收集。
- 剩余：网页专项E2E（草稿响应丢失/冲突、本机缓存保护、多pending继任、FreeTalk与材料管理）、全量check与真实材料只读审计、截图审查/集中修复、最终独立Review和按阶段Commit。
- 用户的声音自然度与10–20项实际学习体验属于外部验收。没有这部分结果，不宣称“好用/提效已被证明”。
旧状态与事故完整保存在 archive/pre-web-usability-2026-09-08/PROJECT_STATUS.md。
