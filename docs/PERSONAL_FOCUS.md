# 真实生成、年轻美式表达与个人重点

状态：2026-09-10现行增量契约；保留SENTENCE_STUDY唯一默认路线，不新增学习模式。实际完成情况只以PROJECT_STATUS为准。

## 本轮范围

1. 修复Responses真实请求、流式结果、严格结构与失败恢复；按用户新增要求切换V4.1 Flash。不能循环原样重试参数错误，也不能把审核拒绝变成成功。
2. 新材料绑定young-us-v1：自然当代美式，保留本人观点和话语功能，不堆俚语。中文决定意图范围，英文提供能力证据；纯英文允许跳过补中文。
3. 默认MiMo Milo/en-US、独立声线和口音、保持音高的播放倍速；不克隆指定播客主持人。两类播放策略保留。
4. 中文/英文准确选区黄色高亮、持久保存、取消和失败恢复；换版可靠映射，不修改学习评分。
5. 审核确认的原错误与纠正进入既有老师记忆；来源/撤销/去重/提示状态完整，普通对话最多自然带入一个旧问题，整题最多两个。原错句折叠、不自动朗读。
6. 十道既有题目离线校准，合格句不重写，独立作者/Reviewer/覆盖证据在私有目录；新增版本保留原文和成绩。

## 费用与发布

真实验证最初8次DeepSeek，用户追加5次再追加6次，最终最多19次；MiMo最多4条。失败也计数，私有账本请求前预占，不无限重试或充值。其余自动化只Mock和隔离库。

按SemVer发布可体验批次，不按每次文件修改升版本：兼容修复patch、兼容功能minor，稳定公共契约不兼容才major；0.x仍是开发阶段。目标v0.2.0，远端占用则另增未用版本，不覆盖标签。

版本、锁文件、应用内显示、CHANGELOG与GitHub Release一致，实际日期和限制如实记录。仅安全独立快照推main，不推私人正文/音频/数据库/Key/敏感旧Git历史。CI失败不得创建声称稳定的Release，未实测不得写通过；项目仍未选定开源许可证。
