# AGENTS.md — 开发者入口
2026-09-08 PO最新调整：以可体验的功能交付为先，验证由开发者按风险判断；本地交付不强制反复全库检查、逐份文档重写或多轮Review。保留关键路线、数据/费用安全检查；CI完整门保持，但未运行须如实说明。历史流程不能扩大本轮工作量。
当前执行：2026-09-10已批准的 v0.2.0（真实生成修复、年轻美式表达与个人重点记忆），句子学习主线不变。先读 docs/README.md、PROJECT_STATUS 和任务相关契约。本轮允许通过独立安全快照发布GitHub与版本说明，不推原敏感历史；安卓、同步和签名冻结，Windows偶发退出不调查。

## 不可降低的工程约束
- 原回答、来源与旧版本保留；编号迁移先备份，稳定 ID 不变，不复活删除内容。
- 私人原件只在 data/imports/private；Key 只在服务端配置，不入 Git、客户端返回、日志和备份。公开 Key 须轮换。
- 自动化仅 Mock 和 test-results 隔离库；不写真实学习测试，不消费 Runtime；VITEST 数据库硬闸门保留。
- Generator → 独立 Reviewer → Coverage Auditor，规则不能批准；不同上下文、请求、runId，AI 输出通过版本化 Zod。
- GET 不创建回答或启动 AI。输入、任务检查点、幂等事件和失败恢复可追溯，失败不冒充通过。
- 接触、自评、四步完成、独立输出分别记账，不把点过/学过当客观掌握。
- 测试、真实内容、真实声音、视觉与学习效果分别留证；模型自述和 Mock 不能替代验收。
- 小步提交；保留被冻结安卓代码、旧工作目录与历史证据，不破坏性回退。

## 当前产品契约
唯一默认路线见 docs/SENTENCE_STUDY.md：整题自然句序，完整中文→看自然表达/句内用法与注意点→三档自评→下一句。无倒计时、五项分组、自动回插或强制输出，随时暂停。
所有旧轻学习、四步、表格等只在 /extensions 主动进入；旧会话不占默认恢复与复习，旧成绩不回填句子。
句子独立日级FSRS、90%保持率；脱口而出/想了一会儿/没想出来映射Good/Hard/Again。最近评分更正用原before与原时刻，不能叠加成功次数。
默认原文先保存，独立生成/审核；自然全文和句子使用同一版本。准备意图与确认错误分开，中文来源真实，模糊/转写问题待确认。
可选单句/整题/独立输出共用Companion。反馈不自动制卡，确认错误独立审核后入可撤销记忆；看过反馈后的修正不能冒充独立输出。
文本按PO本轮追加要求使用DeepSeek V4.1 Flash（API名deepseek-flash），不改写历史模型记录。自然声音mimo-v2.5-tts、Milo/en-US，声线/口音/播放倍率独立；快捷试听缓存或本机声音，音源明示。历史内容开发零Runtime；本轮真实验证原8次文本、PO先追加5次、最后追加6次，总上限19次文本请求与4条MiMo（失败也计数），以私有账本为准，超限停止。
保持蓝色品牌、灰蓝阅读界面和简洁首页。以实际响应与用户体验为准，不用旧截图裁决限制当前改进。

## 技术自主权
界面按用户批准的代码实现流程推进；Skill只取与当前任务相关的方法，不强制图像三方案或反复审查，不修改全局Skill。用户新的产品要求高于历史文档/Skill；数据、费用、隐私和真实证据保护保留。
主执行者负责实现和集成，可委派有明确边界的子任务及独立审核，不让 PO 管理并行开发窗口；同一文件只有一个实现者。
局部实现、模块划分和测试组织由开发者决定；产品目标、费用、隐私用途、公开发布和破坏性操作须另获授权。
数据/证据语义变化先更新相关契约。阶段按风险定向测试、真实交接与Commit；CI保留完整npm run check，本地未运行须说明，不为重复取得相同证据阻塞小改交付。
安卓代码可复用纯业务模块，但不继续安卓适配/构建/同步功能。不能因冻结而删除用户资料或历史迁移。

## 按需手册与交接
可体验批次按SemVer发布，版本/锁文件/应用显示/CHANGELOG/Release同步；不为临时修改升版本。本轮及后续用户授权交付通过安全独立快照更新GitHub，保留实际日期与限制；不得推私人历史、覆盖旧标签或在CI失败时称稳定发布。完整规则见docs/PERSONAL_FOCUS.md。
规则不按模型名称豁免。熟悉项目的执行者自行选择局部结构和测试；新接手者先核对真实状态，再领取有输入/输出/成功与失败行为/定向命令的窄任务。难题可扩大只读排查，不自行扩大产品、付费或公开范围。中断记录实际Commit、脏文件、失败点及下一条命令。
